PORT=3001

# Postgres (Railway provides this automatically)
DATABASE_URL=postgresql://postgres:password@host:5432/railway

# Auth
JWT_SECRET=long-random-string-here
ADMIN_PASSWORD=jack-login-password
ADMIN_API_KEY=clea-api-key-here          # Clea uses this for automated ops
ADMIN_EMAIL=jackcbauerle@gmail.com

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email (SMTP — use Gmail app password or send from billing@bauersoft.io)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=billing@bauersoft.io
SMTP_PASS=app-password-here
EMAIL_FROM=billing@bauersoft.io

# App
APP_URL=https://billing.bauersoft.io
ALLOWED_ORIGINS=https://billing.bauersoft.io,https://bauersoft.io
